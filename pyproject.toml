[project]
name = "fm26-agent"
version = "0.1.0"
description = "FM26 assistant agent — computer-use agent for Football Manager 26"
requires-python = ">=3.12"
dependencies = [
    "pydantic-settings>=2.4",
]

[project.scripts]
agent = "agent.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pyright>=1.1",
    "ruff>=0.4",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["src/agent/tests"]

[tool.pyright]
pythonVersion = "3.12"
pythonPlatform = "Darwin"
include = ["src/agent"]
extraPaths = ["src"]
venvPath = "."
venv = ".venv"
